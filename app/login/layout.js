

import React from 'react'

export const metadata = {
      title: 'login',
  }

const LoginLayout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default LoginLayout
